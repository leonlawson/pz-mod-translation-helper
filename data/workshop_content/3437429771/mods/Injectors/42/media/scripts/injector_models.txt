module injectorModels{
  imports {
	  Base
  }

  model adrenaline-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/adrenaline,
    scale   = 0.17,
  }

  model ahf1-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/ahf1,
    scale   = 0.17,
  }

  model btg2a2-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/btg2a2,
    scale   = 0.17,
  }

  model btg3-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/btg3,
    scale   = 0.17,
  }

  model etg-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/etg,
    scale   = 0.17,
  }

  model meldonin-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/meldonin,
    scale   = 0.17,
  }

  model morphine-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/morphine,
    scale   = 0.17,
  }

  model mule-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/mule,
    scale   = 0.17,
  }

  model norepinephrine-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/norepinephrine,
    scale   = 0.17,
  }

  model obdolbos-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/obdolbos,
    scale   = 0.17,
  }

  model obdolbos2-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/obdolbos2,
    scale   = 0.17,
  }

  model p22-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/p22,
    scale   = 0.17,
  }

  model perfotoran-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/perfotoran,
    scale   = 0.17,
  }

  model pnb-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/pnb,
    scale   = 0.17,
  }

  model propital-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/propital,
    scale   = 0.17,
  }

  model sj1-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/sj1,
    scale   = 0.17,
  }

  model sj6-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/sj6,
    scale   = 0.17,
  }

  model sj9-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/sj9,
    scale   = 0.17,
  }

  model sj12-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/sj9,
    scale   = 0.17,
  }

  model trimadol-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/trimadol,
    scale   = 0.17,
  }

  model xtg-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/xtg,
    scale   = 0.17,
  }

  model zagustin-closed
  {
    mesh    = injectorclosed,
    texture = WorldItems/zagustin,
    scale   = 0.17,
  }

  model injector-empty
  {
    mesh    = injectorclosed,
    texture = WorldItems/empty,
    scale   = 0.17,
  }
}