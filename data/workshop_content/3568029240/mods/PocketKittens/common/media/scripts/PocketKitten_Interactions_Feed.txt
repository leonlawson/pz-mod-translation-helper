module PocketKitten {
    imports {
        Base
    }

    // Legacy action recipes have been removed to ensure Build 42 compatibility.
    // All actions (Adopt, Pet, Feed, etc.) are now handled by TimedActions in the Lua files.

}