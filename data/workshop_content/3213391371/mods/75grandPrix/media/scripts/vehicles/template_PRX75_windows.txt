module Base
{

	model 75grandPrixWindowfl
	{
		mesh = vehicles/Vehicles_75grandPrix_Body|75pontiac_window_fl,
		shader = damn_vehicle_shader,
		static = FALSE,
		scale = 0.1,
		boneWeight = window_fl_bone 1.0,
	}

	model 75grandPrixWindowfr
	{
		mesh = vehicles/Vehicles_75grandPrix_Body|75pontiac_window_fr,
		shader = damn_vehicle_shader,
		static = FALSE,
		scale = 0.1,
		boneWeight = window_fr_bone 1.0,
	}

	model 75grandPrixWindowrl
	{
		mesh = vehicles/Vehicles_75grandPrix_Body|75pontiac_window_rl,
		shader = damn_vehicle_shader,
		scale = 0.1,
	}

	model 75grandPrixWindowrr
	{
		mesh = vehicles/Vehicles_75grandPrix_Body|75pontiac_window_rr,
		shader = damn_vehicle_shader,
		scale = 0.1,
	}

	template vehicle PRX75Windows
	{
		part WindowFrontLeft
		{
			model windowFL
			{
				file = 75grandPrixWindowfl,
			}

			anim ClosedToOpen
			{
				anim = window_fl_opening,
				animate = FALSE,
			}

			parent = DoorFrontLeft,
			area = SeatFrontLeft,
			itemType = Base.75grandPrixFrontSideWindow,
			table install
			{
				requireInstalled = DoorFrontLeft,
			}
			table uninstall
				{
				requireUninstalled = PRX75FrontLeftArmor,
				}
		}

		part WindowFrontRight
		{
			model windowFR
			{
				file = 75grandPrixWindowfr,
			}

			anim ClosedToOpen
			{
				anim = window_fr_opening,
				animate = FALSE,
			}

			parent = DoorFrontRight,
			area = SeatFrontRight,
			itemType = Base.75grandPrixFrontSideWindow,
			table install
			{
				requireInstalled = DoorFrontRight,
			}
			table uninstall
				{
				requireUninstalled = PRX75FrontRightArmor,
				}
		}

		part WindowRearLeft
		{
			model windowRL
			{
				file = 75grandPrixWindowrl,
			}

			area = SeatRearLeft,
			itemType = Base.75grandPrixRearSideWindow,
			table uninstall
				{
				requireUninstalled = PRX75RearLeftArmor,
				}
		}

		part WindowRearRight
		{
			model windowRR
			{
				file = 75grandPrixWindowrr,
			}

			area = SeatRearRight,
			itemType = Base.75grandPrixRearSideWindow,
			table uninstall
				{
				requireUninstalled = PRX75RearRightArmor,
				}
		}

		part Window*
		{
			category = door,
			mechanicRequireKey = true,
			window
			{
				openable = true,
			}
			table install
			{
				items
				{
					1
					{
						type = Base.Screwdriver,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 500,
				skills = Mechanics:3,
				recipes = Intermediate Mechanics,
				test = Vehicles.InstallTest.Default,
				complete = Vehicles.InstallComplete.Window,
			}
			table uninstall
			{
				items
				{
					1
					{
						type = Base.Screwdriver,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 500,
				skills = Mechanics:3,
				recipes = Intermediate Mechanics,
				test = Vehicles.UninstallTest.Default,
				complete = Vehicles.UninstallComplete.Default,
			}
			lua
			{
				create = Vehicles.Create.Window,
				init = Vehicles.Init.Window,
			}
		}
	}

}

