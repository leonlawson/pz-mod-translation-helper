module Base
{
    model Tatrapan_hood
    {
        mesh = vehicles/Tatrapan|Hood,
        shader = vehicle_noreflect,
	static = TRUE,
        scale = 0.01,
	boneWeight = HoodBone 1.0,
    }

    template vehicle Tatrapan_hood
    {
        template = EngineDoor,

        part EngineDoor
        {
            model Default
			{
				file = Tatrapan_hood,
			}

			category = bodywork,
           
            area = Engine,
			mechanicRequireKey = true,
			repairMechanic = true,
			
			anim Open
			{
				sound = VehicleHoodOpenStandard,
			}
			anim Close
			{
				sound = VehicleHoodCloseStandard,
			}
			anim ActorOpen
			{
				anim = WindowOpenSuccess,
				rate = 0.15,
				
			}
			anim ActorClose
			{
				anim = Attack_Shove,
				rate = 0.3,
				
			}

			anim Open
			{
				sound = VehicleHoodOpenVan,
				anim = Hood_closing,
				reverse = TRUE,
				rate = 2.0,
			}

			anim Close
			{
				sound = VehicleHoodCloseVan,
				anim = Hood_closing,
				reverse = FALSE,
				rate = 2.0,
			}

			anim Closed
			{
				anim = Hood_closing,
				reverse = TRUE,
				animate = FALSE,
			}

			anim Opened
			{
				anim = Hood_closing,
				reverse = FALSE,
				animate = FALSE,
			}


		
			table install
			{
				recipes = Intermediate Mechanics,
			}

			table uninstall
			{
				recipes = Intermediate Mechanics,
			}
		


			lua
			{

                update = Vehicles.Update.EngineDoor,
                use = Vehicles.Use.EngineDoor,
			}
        }
    }
}