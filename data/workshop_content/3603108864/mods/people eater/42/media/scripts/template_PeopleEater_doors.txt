module Base
{
    model PeopleEater_DoorFrontLeft
	{
		mesh = vehicles/PeopleEater|doorFL,
		shader = vehicle_noreflect,
		scale = 1,
		static = TRUE,
        boneWeight = FrontLeftDoorBone 1.0,
	}
	model PeopleEater_DoorFrontRight
	{
		mesh = vehicles/PeopleEater|doorFR,
		shader = vehicle_noreflect,
		scale = 1,
		static = TRUE,
        boneWeight = FrontRightDoorBone 1.0,
	}
        model PeopleEater_DoorRearRight
	{
		mesh = vehicles/PeopleEater|doorRR,
		shader = vehicle_noreflect,
		scale = 1,
		static = TRUE,
        boneWeight = RightRearDoorBone 1.0,
	}
        model PeopleEater_DoorRearLeft
	{
		mesh = vehicles/PeopleEater|doorRL,
		shader = vehicle_noreflect,
		scale = 1,
		static = TRUE,
        boneWeight = LeftRearDoorBone 1.0,
	}


	template vehicle PeopleEater_doors
	{
		template = Door/part/DoorFrontLeft,
		template = Door/part/DoorFrontRight,
                template = Door/part/DoorRearRight,
                template = Door/part/DoorRearLeft,

		
		part DoorFrontLeft
		{
			model Default
			{
				file = PeopleEater_DoorFrontLeft,
			}

			area = SeatFrontLeft,
			
            
						

			anim Close
			{
                                sound = VehicleDoorCloseSportsCar,
				anim = DoorFrontLeft_closing,
				reverse = FALSE,
				rate = 2,
			}

			anim Open
			{
                                sound = VehicleDoorOpenSportsCar,
				anim = DoorFrontLeft_closing,
				reverse = TRUE,
				rate = 2,
			}

			anim Closed
			{
				anim = DoorFrontLeft_closing,
				reverse = TRUE,
				animate = FALSE,
			}

			anim Opened
			{
				anim = DoorFrontLeft_closing,
				reverse = FALSE,
				animate = FALSE,
			}

			table uninstall
			{
				requireUninstalled = WindowFrontLeft,
			}
		}

		part DoorFrontRight
		{
			model Default
			{
				file = PeopleEater_DoorFrontRight,
			}

			area = SeatFrontRight,
			

			anim Close
			{
                                sound = VehicleDoorCloseSportsCar,
				anim = DoorFrontRight_closing,
				reverse = FALSE,
				rate = 2,
			}

			anim Open
			{
                                sound = VehicleDoorOpenSportsCar,
				anim = DoorFrontRight_closing,
				reverse = TRUE,
				rate = 2,
			}

			anim Closed
			{
				anim = DoorFrontRight_closing,
				reverse = TRUE,
				animate = FALSE,
			}

			anim Opened
			{
				anim = DoorFrontRight_closing,
				reverse = FALSE,
				animate = FALSE,
			}

			table uninstall
			{
				requireUninstalled = WindowFrontRight,
			}
		}

                part DoorRearLeft
		{
			model Default
			{
				file = PeopleEater_DoorRearLeft,
			}

			area = SeatRearLeft,
			
            
						

			anim Close
			{
                                sound = VehicleDoorCloseSportsCar,
				anim = DoorRearLeft_closing,
				reverse = FALSE,
				rate = 2,
			}

			anim Open
			{
                                sound = VehicleDoorOpenSportsCar,
				anim = DoorRearLeft_closing,
				reverse = TRUE,
				rate = 2,
			}

			anim Closed
			{
				anim = DoorRearLeft_closing,
				reverse = TRUE,
				animate = FALSE,
			}

			anim Opened
			{
				anim = DoorRearLeft_closing,
				reverse = FALSE,
				animate = FALSE,
			}

			table uninstall
			{
				requireUninstalled = WindowRearLeft,
			}
		}

                part DoorRearRight
		{
			model Default
			{
				file = PeopleEater_DoorRearRight,
			}

			area = SeatRearRight,
			
            
						

			anim Close
			{
                                sound = VehicleDoorCloseSportsCar,
				anim = DoorRearRight_closing,
				reverse = FALSE,
				rate = 2,
			}

			anim Open
			{
                                sound = VehicleDoorOpenSportsCar,
				anim = DoorRearRight_closing,
				reverse = TRUE,
				rate = 2,
			}

			anim Closed
			{
				anim = DoorRearRight_closing,
				reverse = TRUE,
				animate = FALSE,
			}

			anim Opened
			{
				anim = DoorRearRight_closing,
				reverse = FALSE,
				animate = FALSE,
			}

			table uninstall
			{
				requireUninstalled = WindowRearRight,
			}
		}
		
                

		part Door*
		{
			category = door,
			mechanicRequireKey = true,

			table install
			{
				test = Vehicles.InstallTest.Default,
				complete = Vehicles.InstallComplete.Door,
			}

			table uninstall
			{
				test = Vehicles.UninstallTest.Default,
				complete = Vehicles.UninstallComplete.Door,
			}

			lua
			{
				create = Vehicles.Create.Door,
				init = Vehicles.Init.Door,
				use = Vehicles.Use.Door,
			}
		}
	}
}