module Base
{

	model 93fordTaurusWindowfl
	{
		mesh = vehicles/Vehicles_93fordTaurus_Body|taurus_window_fl,
		shader = damn_vehicle_shader,
		static = FALSE,
		scale = 0.1,
	}

	model 93fordTaurusWindowfr
	{
		mesh = vehicles/Vehicles_93fordTaurus_Body|taurus_window_fr,
		shader = damn_vehicle_shader,
		static = FALSE,
		scale = 0.1,
	}

	model 93fordTaurusWindowrl
	{
		mesh = vehicles/Vehicles_93fordTaurus_Body|taurus_window_rl,
		shader = damn_vehicle_shader,
		static = FALSE,
		scale = 0.1,
	}

	model 93fordTaurusWindowrr
	{
		mesh = vehicles/Vehicles_93fordTaurus_Body|taurus_window_rr,
		shader = damn_vehicle_shader,
		static = FALSE,
		scale = 0.1,
	}

	model 93fordTaurusWindowbl
	{
		mesh = vehicles/Vehicles_93fordTaurus_Body|taurusWagon_window_bl,
		shader = damn_vehicle_shader,
		scale = 0.1,
	}

	model 93fordTaurusWindowbr
	{
		mesh = vehicles/Vehicles_93fordTaurus_Body|taurusWagon_window_br,
		shader = damn_vehicle_shader,
		scale = 0.1,
	}

	template vehicle TAU93WindowsSedan
	{
		part WindowFrontLeft
		{
			model windowFL
			{
				file = 93fordTaurusWindowfl,
			}

			anim ClosedToOpen
			{
				anim = window_fl_opening,
				animate = FALSE,
			}

			parent = DoorFrontLeft,
			area = SeatFrontLeft,
			itemType = Base.93fordTaurusFrontSideWindow,
			table install
			{
				requireInstalled = DoorFrontLeft,
			}
			table uninstall
				{
				requireUninstalled = TAU93FrontLeftArmor,
				}
		}

		part WindowFrontRight
		{
			model windowFR
			{
				file = 93fordTaurusWindowfr,
			}

			anim ClosedToOpen
			{
				anim = window_fr_opening,
				animate = FALSE,
			}

			parent = DoorFrontRight,
			area = SeatFrontRight,
			itemType = Base.93fordTaurusFrontSideWindow,
			table install
			{
				requireInstalled = DoorFrontRight,
			}
			table uninstall
				{
				requireUninstalled = TAU93FrontRightArmor,
				}
		}

		part WindowRearLeft
		{
			model windowRL
			{
				file = 93fordTaurusWindowrl,
			}

			anim ClosedToOpen
			{
				anim = window_rl_opening,
				animate = FALSE,
			}

			parent = DoorRearLeft,
			area = SeatRearLeft,
			itemType = Base.93fordTaurusRearSideWindow,
			table install
			{
				requireInstalled = DoorRearLeft,
			}
			table uninstall
				{
				requireUninstalled = TAU93RearLeftArmor,
				}
		}

		part WindowRearRight
		{
			model windowRR
			{
				file = 93fordTaurusWindowrr,
			}

			anim ClosedToOpen
			{
				anim = window_rr_opening,
				animate = FALSE,
			}

			parent = DoorRearRight,
			area = SeatRearRight,
			itemType = Base.93fordTaurusRearSideWindow,
			table install
			{
				requireInstalled = DoorRearRight,
			}
			table uninstall
				{
				requireUninstalled = TAU93RearRightArmor,
				}
		}

		part Window*
		{
			category = door,
			mechanicRequireKey = true,
			window
			{
				openable = true,
			}
			table install
			{
				items
				{
					1
					{
						type = Base.Screwdriver,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 500,
				skills = Mechanics:3,
				recipes = Intermediate Mechanics,
				test = Vehicles.InstallTest.Default,
				complete = Vehicles.InstallComplete.Window,
			}
			table uninstall
			{
				items
				{
					1
					{
						type = Base.Screwdriver,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 500,
				skills = Mechanics:3,
				recipes = Intermediate Mechanics,
				test = Vehicles.UninstallTest.Default,
				complete = Vehicles.UninstallComplete.Default,
			}
			lua
			{
				create = Vehicles.Create.Window,
				init = Vehicles.Init.Window,
			}
		}
	}

	template vehicle TAU93WindowsWagon
	{

		template! = TAU93WindowsSedan,

		part WindowBackLeft
		{
			model windowBL
			{
				file = 93fordTaurusWindowbl,
			}

			area = SeatRearLeft,
			itemType = Base.93fordTaurusWagonBackSideWindow,

			table uninstall
				{
				requireUninstalled = TAU93BackLeftArmor,
				}
		}

		part WindowBackRight
		{
			model windowBR
			{
				file = 93fordTaurusWindowbr,
			}

			area = SeatRearRight,
			itemType = Base.93fordTaurusWagonBackSideWindow,

			table uninstall
				{
				requireUninstalled = TAU93BackRightArmor,
				}
		}

		part Window*
		{
			category = door,
			mechanicRequireKey = true,
			window
			{
				openable = true,
			}
			table install
			{
				items
				{
					1
					{
						type = Base.Screwdriver,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 500,
				skills = Mechanics:3,
				recipes = Intermediate Mechanics,
				test = Vehicles.InstallTest.Default,
				complete = Vehicles.InstallComplete.Window,
			}
			table uninstall
			{
				items
				{
					1
					{
						type = Base.Screwdriver,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 500,
				skills = Mechanics:3,
				recipes = Intermediate Mechanics,
				test = Vehicles.UninstallTest.Default,
				complete = Vehicles.UninstallComplete.Default,
			}
			lua
			{
				create = Vehicles.Create.Window,
				init = Vehicles.Init.Window,
			}
		}
	}

}

