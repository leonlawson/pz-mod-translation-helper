module Base
{
    model Tatra815_DoorFrontLeft
	{
		mesh = vehicles/Vehicles_Tatra815_Body|leftfrontdoor,
		shader = vehicle,
		scale = 1,
		static = FALSE,
        boneWeight = FrontLeftDoorBone 1.0,
	}
	model Tatra815_DoorFrontRight
	{
		mesh = vehicles/Vehicles_Tatra815_Body|rightfrontdoor,
		shader = vehicle,
		scale = 1,
		static = FALSE,
        boneWeight = FrontRightDoorBone 1.0,
	}
        model Tatra815_DoorRearRight
	{
		mesh = vehicles/Vehicles_Tatra815_Body|rightreardoor,
		shader = vehicle,
		scale = 1,
		static = FALSE,
        boneWeight = RearRightDoorBone 1.0,
	}
        model Tatra815_DoorRearLeft
	{
		mesh = vehicles/Vehicles_Tatra815_Body|leftreardoor,
		shader = vehicle,
		scale = 1,
		static = FALSE,
        boneWeight = RearLeftDoorBone 1.0,
	}
        model Tatra815_DoorMiddleLeft
	{
		mesh = vehicles/Vehicles_Tatra815_Body|fronthatch,
		shader = vehicle,
		scale = 1,
		static = FALSE,
        boneWeight = FrontHatchBone 1.0,
	}
        model Tatra815_DoorMiddleRight
	{
		mesh = vehicles/Vehicles_Tatra815_Body|rearhatch,
		shader = vehicle,
		scale = 1,
		static = FALSE,
        boneWeight = RearHatchBone 1.0,
	}
	

	template vehicle Tatra815_doors
	{
		template = Door/part/DoorFrontLeft,
		template = Door/part/DoorFrontRight,
                template = Door/part/DoorRearRight,
                template = Door/part/DoorRearLeft,
                template = Door/part/DoorMiddleLeft,
                template = Door/part/DoorMiddleRight,

		
		part DoorFrontLeft
		{
			model Default
			{
				file = Tatra815_DoorFrontLeft,
			}

			area = SeatFrontLeft,
			
            
						

			anim Close
			{
                                sound = VehicleDoorCloseSportsCar,
				anim = DoorFrontLeft_closing,
				reverse = FALSE,
				rate = 2,
			}

			anim Open
			{
                                sound = VehicleDoorOpenSportsCar,
				anim = DoorFrontLeft_closing,
				reverse = TRUE,
				rate = 2,
			}

			anim Closed
			{
				anim = DoorFrontLeft_closing,
				reverse = TRUE,
				animate = FALSE,
			}

			anim Opened
			{
				anim = DoorFrontLeft_closing,
				reverse = FALSE,
				animate = FALSE,
			}

			table uninstall
			{
				requireUninstalled = WindowFrontLeft,
			}
		}

		part DoorFrontRight
		{
			model Default
			{
				file = Tatra815_DoorFrontRight,
			}

			area = SeatFrontRight,
			

			anim Close
			{
                                sound = VehicleDoorCloseSportsCar,
				anim = DoorFrontRight_closing,
				reverse = FALSE,
				rate = 2,
			}

			anim Open
			{
                                sound = VehicleDoorOpenSportsCar,
				anim = DoorFrontRight_closing,
				reverse = TRUE,
				rate = 2,
			}

			anim Closed
			{
				anim = DoorFrontRight_closing,
				reverse = TRUE,
				animate = FALSE,
			}

			anim Opened
			{
				anim = DoorFrontRight_closing,
				reverse = FALSE,
				animate = FALSE,
			}

			table uninstall
			{
				requireUninstalled = WindowFrontRight,
			}
		}

                part DoorRearLeft
		{
			model Default
			{
				file = Tatra815_DoorRearLeft,
			}

			area = SeatRearLeft,
			
            
						

			anim Close
			{
                                sound = VehicleDoorCloseSportsCar,
				anim = DoorRearLeft_closing,
				reverse = FALSE,
				rate = 2,
			}

			anim Open
			{
                                sound = VehicleDoorOpenSportsCar,
				anim = DoorRearLeft_closing,
				reverse = TRUE,
				rate = 2,
			}

			anim Closed
			{
				anim = DoorRearLeft_closing,
				reverse = TRUE,
				animate = FALSE,
			}

			anim Opened
			{
				anim = DoorRearLeft_closing,
				reverse = FALSE,
				animate = FALSE,
			}

			table uninstall
			{
				requireUninstalled = WindowRearLeft,
			}
		}

                part DoorRearRight
		{
			model Default
			{
				file = Tatra815_DoorRearRight,
			}

			area = SeatRearRight,
			
            
						

			anim Close
			{
                                sound = VehicleDoorCloseSportsCar,
				anim = DoorRearRight_closing,
				reverse = FALSE,
				rate = 2,
			}

			anim Open
			{
                                sound = VehicleDoorOpenSportsCar,
				anim = DoorRearRight_closing,
				reverse = TRUE,
				rate = 2,
			}

			anim Closed
			{
				anim = DoorRearRight_closing,
				reverse = TRUE,
				animate = FALSE,
			}

			anim Opened
			{
				anim = DoorRearRight_closing,
				reverse = FALSE,
				animate = FALSE,
			}

			table uninstall
			{
				requireUninstalled = WindowRearRight,
			}
		}
		
                part DoorMiddleLeft
		{
			model Default
			{
				file = Tatra815_DoorMiddleLeft,
			}

			area = SeatMiddleLeft,
			
            
						

	

			table uninstall
			{
				requireUninstalled = WindowMiddleLeft,
			}
		}

                part DoorMiddleRight
		{
			model Default
			{
				file = Tatra815_DoorMiddleRight,
			}

			area = SeatMiddleRight,
			
            
						

	

			table uninstall
			{
				requireUninstalled = WindowMiddleRight,
			}
		}

		part Door*
		{
			category = door,
			mechanicRequireKey = true,

			table install
			{
				test = Vehicles.InstallTest.Default,
				complete = Vehicles.InstallComplete.Door,
			}

			table uninstall
			{
				test = Vehicles.UninstallTest.Default,
				complete = Vehicles.UninstallComplete.Door,
			}

			lua
			{
				create = Vehicles.Create.Door,
				init = Vehicles.Init.Door,
				use = Vehicles.Use.Door,
			}
		}
	}
}