module Base
{

	model 91nissan240sxWindowfl
	{
		mesh = vehicles/Vehicles_91nissan240sx_Body|240sx_window_fl,
		shader = damn_vehicle_shader,
		static = FALSE,
		scale = 0.1,
        boneWeight = window_fl_bone 1.0,
	}

	model 91nissan240sxWindowfr
	{
		mesh = vehicles/Vehicles_91nissan240sx_Body|240sx_window_fr,
		shader = damn_vehicle_shader,
		static = FALSE,
		scale = 0.1,
        boneWeight = window_fr_bone 1.0,
	}

    model 91nissan240sxWindowrl
	{
		mesh = vehicles/Vehicles_91nissan240sx_Body|240sx_window_rl,
		shader = damn_vehicle_shader,
		scale = 0.1,
	}

	model 91nissan240sxWindowrr
	{
		mesh = vehicles/Vehicles_91nissan240sx_Body|240sx_window_rr,
		shader = damn_vehicle_shader,
		scale = 0.1,
	}

	template vehicle N240Windows
	{
		part WindowFrontLeft
		{
			model windowFL
			{
				file = 91nissan240sxWindowfl,
			}

			anim ClosedToOpen
			{
				anim = window_fl_opening,
				animate = FALSE,
			}

			parent = DoorFrontLeft,
			area = SeatFrontLeft,
			itemType = Base.91nissan240sxFrontSideWindow,
			table install
			{
				requireInstalled = DoorFrontLeft,
			}
			table uninstall
				{
				requireUninstalled = DAMNFrontLeftArmor,
				}
		}

		part WindowFrontRight
		{
			model windowFR
			{
				file = 91nissan240sxWindowfr,
			}

			anim ClosedToOpen
			{
				anim = window_fr_opening,
				animate = FALSE,
			}

			parent = DoorFrontRight,
			area = SeatFrontRight,
			itemType = Base.91nissan240sxFrontSideWindow,
			table install
			{
				requireInstalled = DoorFrontRight,
			}
			table uninstall
				{
				requireUninstalled = DAMNFrontRightArmor,
				}
		}

		part WindowFront*
		{
			category = door,
			mechanicRequireKey = true,
			durability = 2,
			window
			{
				openable = true,
			}
			table install
			{
				items
				{
					1
					{
						type = Base.Screwdriver,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 500,
				skills = Mechanics:3,
				recipes = Intermediate Mechanics,
				test = Vehicles.InstallTest.Default,
				complete = Vehicles.InstallComplete.Window,
			}
			table uninstall
			{
				items
				{
					1
					{
						type = Base.Screwdriver,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 500,
				skills = Mechanics:3,
				recipes = Intermediate Mechanics,
				test = Vehicles.UninstallTest.Default,
				complete = Vehicles.UninstallComplete.Default,
			}
			lua
			{
				create = Vehicles.Create.Window,
				init = Vehicles.Init.Window,
			}
		}

        part WindowRearLeft
		{
			model windowRL
			{
				file = 91nissan240sxWindowrl,
			}

			area = SeatRearLeft,
			itemType = Base.91nissan240sxRearSideWindow,
			table uninstall
				{
				requireUninstalled = DAMNRearLeftArmor,
				}
		}

		part WindowRearRight
		{
			model windowRR
			{
				file = 91nissan240sxWindowrr,
			}

			area = SeatRearRight,
			itemType = Base.91nissan240sxRearSideWindow,
			table uninstall
				{
				requireUninstalled = DAMNRearRightArmor,
				}
		}

		part WindowRear*
		{
			category = door,
			mechanicRequireKey = true,
			durability = 2,
			window
			{
				openable = false,
			}
			table install
			{
				items
				{
					1
					{
						type = Base.Screwdriver,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 500,
				skills = Mechanics:3,
				recipes = Intermediate Mechanics,
				test = Vehicles.InstallTest.Default,
				complete = Vehicles.InstallComplete.Window,
			}
			table uninstall
			{
				items
				{
					1
					{
						type = Base.Screwdriver,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 500,
				skills = Mechanics:3,
				recipes = Intermediate Mechanics,
				test = Vehicles.UninstallTest.Default,
				complete = Vehicles.UninstallComplete.Default,
			}
			lua
			{
				create = Vehicles.Create.Window,
				init = Vehicles.Init.Window,
			}
		}
	}

}

