module Base
{
	model 91geoMetrofrontBumper0
	{
		mesh = vehicles/Vehicles_91geoMetro_Body|geo_bumper_front0,
		shader = damn_vehicle_shader,
		scale = 0.1,
	}

	model 91geoMetrofrontBumper1
	{
		mesh = vehicles/Vehicles_91geoMetro_Body|geo_bumper_front_a,
		shader = damn_vehicle_shader,
		scale = 0.1,
	}

	model 91geoMetrorearBumper0
	{
		mesh = vehicles/Vehicles_91geoMetro_Body|geo_bumper_rear0,
		shader = damn_vehicle_shader,
		scale = 0.1,
	}

	template vehicle GEO91Bumpers
		{
		part DAMNBumperFront
        	{

			model FrontBumper0
				{
                		file = 91geoMetrofrontBumper0,
				}

			model FrontBumper1
				{
                		file = 91geoMetrofrontBumper1,
				}

				area = Engine,
				category = bodywork,
				specificItem = false,
				itemType = Base.91geoMetroBumperFront0;Base.91geoMetroReinforcedBumperFront1,
				mechanicRequireKey = false,
                durability = 5,

				table install
				{
                	items
                	{
                    	1
                    		{
                        	type = Base.Wrench,
                        	count = 1,
                        	keep = true,
							equip = primary,
                    		}
                	}

                	time = 450,
                	skills = Mechanics:3,
                	recipes = Intermediate Mechanics,
                	test = Vehicles.InstallTest.Default,
                	complete = GEO91.InstallComplete.BumperFront,
            		}

            		table uninstall
            		{
                	items
                	{
                    	1
                    		{
			  				type = Base.Wrench,
							count = 1,
							keep = true,
							equip = primary,
                    		}
                	}

					time = 500,
					skills = Mechanics:3,
					recipes = Intermediate Mechanics,
                	test = Vehicles.UninstallTest.Default,
					complete = GEO91.UninstallComplete.BumperFront,
            		}

            		lua
            		{
					create = GEO91.Create.BumperFront,
					init = GEO91.Init.BumperFront,
					}
		}

		part DAMNBumperRear
        	{

			model RearBumper0
				{
                		file = 91geoMetrorearBumper0,
				}

				area = TruckBed,
				category = bodywork,
				specificItem = false,
				itemType = Base.91geoMetroBumperRear0,
				mechanicRequireKey = false,
                durability = 5,

				table install
				{
                	items
                	{
                    	1
                    		{
                        	type = Base.Wrench,
                        	count = 1,
                        	keep = true,
							equip = primary,
                    		}
                	}

                	time = 450,
                	skills = Mechanics:3,
                	recipes = Intermediate Mechanics,
                	test = Vehicles.InstallTest.Default,
                	complete = GEO91.InstallComplete.BumperRear,
            		}

            		table uninstall
            		{
                	items
                	{
                    	1
                    		{
			  				type = Base.Wrench,
							count = 1,
							keep = true,
							equip = primary,
                    		}
                	}

					time = 500,
					skills = Mechanics:3,
					recipes = Intermediate Mechanics,
                	test = Vehicles.UninstallTest.Default,
					complete = GEO91.UninstallComplete.BumperRear,
            		}

            		lua
            		{
					create = GEO91.Create.BumperRear,
					init = GEO91.Init.BumperRear,
					}
		}
	}

		
}
