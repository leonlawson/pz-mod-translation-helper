module Base
{

	model 98stageaWindowfl
	{
		mesh = vehicles/Vehicles_98stagea_Body|stagea_window_fl,
		shader = damn_vehicle_shader,
		static = FALSE,
		scale = 0.1,
        boneWeight = window_fl_bone 1.0,
	}

	model 98stageaWindowfr
	{
		mesh = vehicles/Vehicles_98stagea_Body|stagea_window_fr,
		shader = damn_vehicle_shader,
		static = FALSE,
		scale = 0.1,
        boneWeight = window_fr_bone 1.0,
	}

	model 98stageaWindowrl
	{
		mesh = vehicles/Vehicles_98stagea_Body|stagea_window_rl,
		shader = damn_vehicle_shader,
		static = FALSE,
		scale = 0.1,
        boneWeight = window_rl_bone 1.0,
	}

	model 98stageaWindowrr
	{
		mesh = vehicles/Vehicles_98stagea_Body|stagea_window_rr,
		shader = damn_vehicle_shader,
		static = FALSE,
		scale = 0.1,
        boneWeight = window_rr_bone 1.0,
	}

    model 98stageaWindowbl
	{
		mesh = vehicles/Vehicles_98stagea_Body|stagea_window_bl,
		shader = damn_vehicle_shader,
		scale = 0.1,
	}

	model 98stageaWindowbr
	{
		mesh = vehicles/Vehicles_98stagea_Body|stagea_window_br,
		shader = damn_vehicle_shader,
		scale = 0.1,
	}

	template vehicle STAGEAWindows
	{
		part WindowFrontLeft
		{
			model windowFL
			{
				file = 98stageaWindowfl,
			}

			anim ClosedToOpen
			{
				anim = window_fl_opening,
				animate = FALSE,
			}

			parent = DoorFrontLeft,
			area = SeatFrontLeft,
			itemType = Base.98stageaFrontSideWindow,
			table install
			{
				requireInstalled = DoorFrontLeft,
			}
			table uninstall
				{
				requireUninstalled = DAMNFrontLeftArmor,
				}
		}

		part WindowFrontRight
		{
			model windowFR
			{
				file = 98stageaWindowfr,
			}

			anim ClosedToOpen
			{
				anim = window_fr_opening,
				animate = FALSE,
			}

			parent = DoorFrontRight,
			area = SeatFrontRight,
			itemType = Base.98stageaFrontSideWindow,
			table install
			{
				requireInstalled = DoorFrontRight,
			}
			table uninstall
				{
				requireUninstalled = DAMNFrontRightArmor,
				}
		}

		part WindowRearLeft
		{
			model windowRL
			{
				file = 98stageaWindowrl,
			}

			anim ClosedToOpen
			{
				anim = window_rl_opening,
				animate = FALSE,
			}

			parent = DoorRearLeft,
			area = SeatRearLeft,
			itemType = Base.98stageaRearSideWindow,
			table install
			{
				requireInstalled = DoorRearLeft,
			}
			table uninstall
				{
				requireUninstalled = DAMNRearLeftArmor,
				}
		}

		part WindowRearRight
		{
			model windowRR
			{
				file = 98stageaWindowrr,
			}

			anim ClosedToOpen
			{
				anim = window_rr_opening,
				animate = FALSE,
			}

			parent = DoorRearRight,
			area = SeatRearRight,
			itemType = Base.98stageaRearSideWindow,
			table install
			{
				requireInstalled = DoorRearRight,
			}
			table uninstall
				{
				requireUninstalled = DAMNRearRightArmor,
				}
		}

        part WindowBackLeft
		{
			model windowBL
			{
				file = 98stageaWindowbl,
			}

			area = SeatRearLeft,
			itemType = Base.98stageaBackSideWindow,

			table uninstall
				{
				requireUninstalled = DAMNBackLeftArmor,
				}
		}

		part WindowBackRight
		{
			model windowBR
			{
				file = 98stageaWindowbr,
			}

			area = SeatRearRight,
			itemType = Base.98stageaBackSideWindow,

			table uninstall
				{
				requireUninstalled = DAMNBackRightArmor,
				}
		}

		part Window*
		{
			category = door,
			mechanicRequireKey = true,
            durability = 2,
			window
			{
				openable = true,
			}
			table install
			{
				items
				{
					1
					{
						type = Base.Screwdriver,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 500,
				skills = Mechanics:3,
				recipes = Intermediate Mechanics,
				test = Vehicles.InstallTest.Default,
				complete = Vehicles.InstallComplete.Window,
			}
			table uninstall
			{
				items
				{
					1
					{
						type = Base.Screwdriver,
						count = 1,
						keep = true,
						equip = primary,
					}
				}
				time = 500,
				skills = Mechanics:3,
				recipes = Intermediate Mechanics,
				test = Vehicles.UninstallTest.Default,
				complete = Vehicles.UninstallComplete.Default,
			}
			lua
			{
				create = Vehicles.Create.Window,
				init = Vehicles.Init.Window,
			}
		}
	}
}